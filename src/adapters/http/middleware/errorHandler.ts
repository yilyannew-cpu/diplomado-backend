/**
 * Reexporta el middleware centralizado.
 * Preferir: `import { handleError } from '../../middlewares/handleError'`
 */
export { handleError as errorHandler } from '../../../middlewares/handleError';
